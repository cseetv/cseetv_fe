# 🔔 브라우저 알림 기능 가이드

## 개요

위험 감지 시 브라우저 자체에서 PC 또는 모바일 기기로 알림을 보내는 기능입니다.

## 기능

- **실시간 알림**: 위험 감지 시 즉시 알림
- **중복 방지**: 연속된 알림 중복 방지 (2초 간격)
- **소리 및 진동**: 경보음 재생 및 모바일 기기 진동
- **알림 유지**: 사용자가 닫을 때까지 유지 (`requireInteraction: true`)
- **오프라인 지원**: Service Worker를 통한 오프라인 알림 (설정 가능)

## 사용 방법

### 1️⃣ 알림 권한 요청

1. 상단 헤더에서 **"🔔 알림 요청"** 버튼 클릭
2. 브라우저 권한 요청 팝업 → **"허용"** 선택
3. 버튼이 **"🔔 알림 ✓"** 로 변경되면 준비 완료

### 2️⃣ 모드 선택

- **라이브 카메라**: 카메라 실시간 모니터링
- **비디오 분석**: 업로드한 영상 분석

### 3️⃣ 위험 감지 시 자동 알림

위험이 감지되면 다음과 같이 알림을 받습니다:

#### 위험도에 따른 알림

- **⚠️ 경고**: 위험도 40~70% (황색)
- **🚨 위험**: 위험도 70% 이상 (적색)

#### 알림 내용

```
제목: 🚨 위험 감지! (또는 ⚠️ 경고)
본문:
  위험도: XX%
  모션: X,XXXpx
```

## 브라우저별 지원

| 브라우저 | PC          | 모바일 |
| -------- | ----------- | ------ |
| Chrome   | ✅          | ✅     |
| Firefox  | ✅          | ✅     |
| Edge     | ✅          | ✅     |
| Safari   | ⚠️ (제한적) | ❌     |

## 설정 및 커스터마이징

### 소리/진동 설정

`src/hooks/useNotification.ts`의 `notifyDanger` 함수에서:

```typescript
await notify({
  title: isDanger ? "🚨 위험 감지!" : "⚠️ 경고",
  body: `위험도: ${riskScore.toFixed(0)}%${details ? `\n${details}` : ""}`,
  tag: "motion-alert",
  sound: true, // 소리 활성화
  vibrate: true, // 진동 활성화
});
```

### 알림 중복 방지 간격 조정

`useNotification.ts`의 `canSendNotification` 함수:

```typescript
if (now - lastTime < 2000) return false; // 2000ms = 2초
```

## PC에서 알림을 받지 못하는 경우

### 1. 브라우저 알림 전역 설정 확인

- **Chrome**: 설정 → 개인정보 및 보안 → 사이트 설정 → 알림
- **Firefox**: 기본 설정 → 권한 → 알림 확인
- **Edge**: 설정 → 개인정보, 검색 및 서비스 → 사이트 권한 → 알림

### 2. 사이트별 알림 권한 확인

현재 사이트가 "허용"으로 설정되어 있는지 확인

### 3. OS 알림 설정 확인

- **Windows**: 설정 → 시스템 → 알림 및 작업
- **macOS**: 시스템 환경설정 → 알림

## 모바일 기기 알림 설정

### iOS (Safari 미지원)

- Chrome/Firefox 앱에서만 사용 가능
- 앱 설정 → 알림 권한 허용

### Android

- Chrome/Firefox 앱에서 사용
- 앱 설정 → 알림 → 해당 사이트 허용

## 기술 상세

### 알림 API

- **Notification API**: 브라우저 기본 알림
- **Service Worker**: 백그라운드 알림 (오프라인)
- **Web Audio API**: 경보음 생성
- **Vibration API**: 모바일 진동

### 알림 트리거 조건

```typescript
if (risk > settings.alert_threshold) {
  // 알림 발송
  // 기본값: alert_threshold = 60 (60% 이상의 위험도)
}
```

## 문제 해결

### 알림이 나타나지 않음

1. 브라우저 권한 확인
2. OS 알림 설정 확인
3. 개발자 도구 콘솔 확인 (F12)

### 소리가 재생되지 않음

- 브라우저/OS 볼륨 확인
- 사이트의 오디오 권한 확인
- 개발자 도구 → Application → Service Workers 확인

### 중복 알림

- 2초 이상 간격으로 감지되는 알림만 발생
- 빠른 연속 감지는 1개만 알림됨

## 팁 & 베스트 프랙티스

✅ **추천**

- 브라우저를 항상 켜두기
- 중요한 시간대에 모니터링
- 정기적으로 권한 설정 확인

❌ **비추천**

- 많은 탭에서 동시 모니터링 (성능 저하)
- 민감한 alert_threshold 설정 (과도한 알림)

## 추가 기능 예정

- [ ] 이메일 알림
- [ ] SMS 알림
- [ ] Slack/Discord 웹훅
- [ ] 커스텀 알림 사운드
- [ ] 알림 히스토리
